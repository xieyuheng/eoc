#!/usr/bin/env -S npx @xieyuheng/x-lisp.js run --debug=true

(import-all "src/index.lisp")

(match (current-command-line-args)
  (['compile input]
   (= file (symbol-to-string input))
   (= x86-program (compile-program (load-program file)))
   (write (format-x86-program x86-program)))
  (['trace-compile input]
   (= file (symbol-to-string input))
   (trace-compile (load-program file)))
  (['help]
   (print-help))
  (_
   (print-help)))

(define (load-program file)
  (parse-program
   `(program
     ()
     ,(parse-sexp (file-get file)))))

(define (print-help)
  (writeln "commands:")
  (writeln "  compile <file> -- compile a file")
  (writeln "  trace-compile <file> -- trace all passes for snapshot testing")
  (writeln "  help -- display help information")
  (writeln "")
  (writeln "default command: help"))
